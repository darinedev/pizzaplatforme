
spring:
  application:
    name: PizzaPlatforme
  datasource:
    url: jdbc:mysql://localhost:3306/pizzaplatforme?createDatabaseIfNotExist=true
    username: pizzauser
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
